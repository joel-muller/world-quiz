services:
  server:
    build: ./server
    ports:
      - "8080:8080"

  client:
    build: ./client
    ports:
      - "4200:8080"
    environment:
      NODE_ENV: development
    depends_on:
      - server
